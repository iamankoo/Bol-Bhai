services:
  server:
    build:
      context: ..
      dockerfile: apps/server/Dockerfile
    ports:
      - "4000:4000"
    environment:
      PORT: "4000"
      HOST: "0.0.0.0"
      LOG_LEVEL: "info"
      CORS_ALLOWED_ORIGINS: ""
    restart: unless-stopped
