# Architecture

See the root [README.md](../README.md) for the current, authoritative
architecture overview (extension/server layout, the room → Socket.IO →
WebRTC signaling flow, the shared `@bol-bhai/shared` protocol package, and
production deployment). This file is intentionally kept short rather than
duplicated and left to drift out of sync — update the README when the
architecture changes.
